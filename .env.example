# Supabase project credentials (Project Settings > API in the Supabase dashboard).
# The anon key is safe for the browser; never put the service role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
